# Serverokey 🚀 v4.3 — Архитектура вместо Кода

**Serverokey** (Сервер-окей) — это атомарный, декларативный движок для бэкенда на Node.js, спроектированный для создания веб-приложений с помощью LLM (Large Language Models). Его главная задача — превратить LLM из "программиста" в "архитектора", который описывает **ЧТО** нужно сделать, а не **КАК**.

Serverokey построен на принципе **"HTML-over-the-wire"**, но с интеллектуальным подходом, позволяющим создавать сложные, интерактивные интерфейсы без написания единой строчки JavaScript на фронтенде.

*   **Автор:** Xzdes
*   **Контакт:** [xzdes@yandex.ru](mailto:xzdes@yandex.ru)
*   **Репозиторий:** [https://github.com/Xzdes/serverokey](httpss://github.com/Xzdes/serverokey)

---

## 🎯 Философия: Архитектура вместо Кода

Современные LLM отлично генерируют код, но часто ошибаются в деталях, требующих контекста. Serverokey решает эту проблему, заменяя написание императивного кода на заполнение декларативных структур в центральном файле — `manifest.js`.

Это позволяет LLM сосредоточиться на своих сильных сторонах — проектировании архитектуры и потоков данных, — минимизируя вероятность ошибок и повышая предсказуемость результата.

1.  **Декларативные Коннекторы:** Вместо написания кода для подключения к БД, вы описываете подключение: `{"type": "wise-json", "collection": "users"}`. Это позволяет менять источник данных (с JSON на `wise-json-db`), не переписывая логику приложения.

2.  **Декларативная Логика (`steps`):** Вместо `if/else` в отдельном JS-файле, вы описываете сложную бизнес-логику прямо в манифесте: `{"if": "receipt.total > 300", "then": [...]}`. Это делает логику прозрачной и легко читаемой.

3.  **Полная Изоляция:** Движок абстрагирует всю сложную "обвязку". Он сам находит файлы, обновляет DOM на клиенте (сохраняя фокус и состояние), управляет состоянием на сервере и обеспечивает безопасность. LLM не имеет прямого доступа к файловой системе или опасным API.

> **Результат:** LLM работает с **единой точкой входа** (`manifest.js`), генерируя предсказуемые, безопасные и легко проверяемые веб-приложения.

---

## 🛠️ Рабочий процесс: От Идеи до Результата

Serverokey предлагает уникальный рабочий процесс, идеально подходящий для быстрой итеративной разработки, в том числе с использованием AI-ассистентов.

### 1. Установка и запуск примера

Проект представляет собой монорепозиторий с двумя основными пакетами:
*   `packages/serverokey`: 📦 Ядро движка.
*   `packages/kassa-app-example`:  Пример приложения, использующего `serverokey`.

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/Xzdes/serverokey.git
cd serverokey

# 2. Установите все зависимости для всех пакетов из корня
npm install

# 3. Перейдите в папку с примером
cd packages/kassa-app-example

# 4. Наполните базу данных wise-json-db начальными данными
npm run seed

# 5. Запустите сервер в режиме разработки
npm run dev
```
Откройте `http://localhost:3000` в браузере. Приложение полностью готово к работе!

### 2. Цикл разработки "Манифест -> Валидатор -> Результат"

Это сердце рабочего процесса Serverokey.

#### **Шаг 1: Редактирование `manifest.js`**
Любое изменение в приложении — добавление страницы, нового действия или источника данных — начинается с редактирования файла `manifest.js`.

#### **Шаг 2: Валидация (Критически важный шаг!)**
**Перед тем как проверять результат в браузере**, запустите встроенный "Супер-Валидатор". Он — ваш главный помощник и система безопасности. Он проверит архитектуру вашего приложения на десятки потенциальных ошибок: от простых опечаток до сложных логических несоответствий.

```bash
# Находясь в папке приложения (например, kassa-app-example)
npm run validate
```
*   **Если вы видите:** `✅ Manifest validation successful. No issues found.` — значит, архитектура корректна, и можно переходить к следующему шагу.
*   **Если вы видите:** `🚨 Found X issues:` — валидатор предоставит вам **четкий и понятный список проблем** с указанием файла, секции и даже предложит исправление (`Did you mean...?`). Исправьте самую верхнюю, фундаментальную ошибку, и запустите валидатор снова.

#### **Шаг 3: Запуск и отладка**
После успешной валидации запустите сервер в режиме отладки. Он включает горячую перезагрузку и выводит массу полезной информации в консоль сервера и браузера.

```bash
npm run dev:debug
```
*   **Горячая перезагрузка:** `nodemon` автоматически перезапустит сервер при изменении любого файла (`.js`, `.html`, `.css`, `manifest.js`).
*   **Режим отладки:** Вы увидите подробные логи о каждом действии, контекст данных для каждого компонента и полный `payload`, отправляемый на клиент. Это бесценно для отладки логики в `steps`.

---

## 📖 Справочник по `manifest.js`

`manifest.js` — это единый источник правды для всего приложения.

### `globals`
Глобальные переменные, доступные во всех шаблонах.

```javascript
globals: {
  appName: "Моя Касса",
  // Делает коннектор 'user' доступным глобально во всех шаблонах
  injectData: ['user'] 
}
```

### `connectors`
Описывает все источники данных. **Это ключевая особенность Serverokey!** Вы можете легко переключаться между типами хранения, просто изменив `type`.

#### Тип: `wise-json` (Рекомендуемый)
Хранит состояние в быстрой и надежной встроенной базе данных `wise-json-db`. Идеально для постоянного хранения данных. Каждый коннектор управляет одним документом-состоянием в своей коллекции.

```javascript
receipt: {
  type: 'wise-json',
  collection: 'receipt', // Имя коллекции в wise-json-db
  initialState: { 
    items: [],
    discountPercent: 10,
    // ... другие поля по умолчанию
  }
}
```

#### Тип: `json`
Хранит состояние в простом JSON-файле в папке `app/data/`. Подходит для простых данных, которые не меняются слишком часто.

```javascript
positions: {
  type: 'json',
  initialState: { items: [...] } 
}
```

#### Тип: `in-memory`
Хранит состояние в оперативной памяти. Данные **сбрасываются** при каждом перезапуске сервера. Идеально для временных данных, таких как состояние UI (например, текст в поле поиска).

```javascript
viewState: {
  type: 'in-memory',
  initialState: { query: '', filtered: [] }
}
```

### `components`
Регистрирует UI-компоненты. Стили, указанные здесь, автоматически становятся изолированными (scoped).

```javascript
components: {
  mainLayout: 'main-layout.html',
  receipt: {
    template: 'receipt.html',   // Шаблон компонента
    style: 'receipt.css'        // Связанный файл стилей
  }
}
```
В CSS-файле используйте псевдоселектор `:host` для стилизации корневого элемента компонента.

### `routes`
Связывает URL и HTTP-метод с действием в системе.

#### `type: 'view'`
Рендерит целую HTML-страницу.
```javascript
'GET /': {
  type: 'view',
  layout: 'mainLayout',
  reads: ['user', 'receipt'], 
  inject: { 'placeholderName': 'componentName' }
}
```

#### `type: 'action'`
Выполняет серверную логику и возвращает обновленный HTML-фрагмент.

**`steps` (Рекомендуемый и самый мощный способ):**
Массив шагов для описания сложной бизнес-логики без написания JS.

```javascript
'POST /action/addItem': {
  type: 'action',
  steps: [
    // 1. Найти нужные данные в контексте
    { "set": "context.productToAdd", "to": "positions.items.find(p => p.id == body.id)" },
    { "set": "context.itemInReceipt", "to": "receipt.items.find(i => i.id == body.id)" },
    // 2. Выполнить условную логику
    {
      "if": "context.itemInReceipt",
      "then": [ { "set": "context.itemInReceipt.quantity", "to": "context.itemInReceipt.quantity + 1" } ],
      "else": [
        { "set": "context.productToAdd.quantity", "to": "1" },
        { "set": "receipt.items", "to": "receipt.items.concat([context.productToAdd])" }
      ]
    },
    // 3. Вызвать "макрос" - другой массив шагов для пересчета
    ...require('./app/actions/recalculateReceipt.js')
  ],
  reads: ['positions', 'receipt'], // Какие данные загрузить в контекст
  writes: ['receipt'],            // Какие данные сохранить после выполнения
  update: 'receipt'               // Какой компонент перерисовать на клиенте
}
```
**Другие способы (менее предпочтительные):**
*   **`handler`:** Ссылка на JS-файл в `app/actions/` для сложной императивной логики.
*   **`manipulate`:** Декларативное описание простой операции (push, remove). Было заменено на `steps` для большей гибкости.

---

## 템 Шаблоны и Клиентская часть

Движок использует **Mustache.js** для рендеринга. Дополнительно предоставляются свои директивы.

*   **Атрибуты взаимодействия:**
    *   `atom-action="METHOD /url"`: Указывает, какой экшен вызвать.
    *   `atom-target="#css-selector"`: Указывает, какой контейнер на странице обновить.
    *   `atom-event="input"`: (Опционально) Указывает, по какому событию вызывать экшен (по умолчанию `click` или `submit`).
*   **Условный рендеринг: `atom-if`**
    Атрибут, который полностью удаляет элемент из DOM, если JavaScript-условие в его значении ложно.
    ```html
    <div class="empty-state" atom-if="!receipt.items.length">Чек пуст</div>
    ```

---

## 🔮 Будущее развитие

*   [ ] **Декларативная аутентификация:** Добавление встроенных `auth` роутов для регистрации/логина.
*   [ ] **Расширенные коннекторы данных:** Добавление встроенных коннекторов для **PostgreSQL**, **SQLite** и декларативного выполнения SQL-запросов.
*   [ ] **Интерактивный CLI-помощник:** Утилита командной строки (`npm run new component ...`) для быстрого создания "атомов" приложения, которая автоматически обновляет `manifest.js`.
*   [ ] **Декларативные WebSocket'ы:** Описание каналов и событий для real-time обновлений.

---

Этот проект — эксперимент в области взаимодействия человека и машины, направленный на создание надежных и предсказуемых программных систем. Буду рад вашим идеям и вкладу!

**Xzdes**