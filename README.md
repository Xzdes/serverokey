# Serverokey 🚀 — Архитектура вместо Кода

**Serverokey** (Сервер-окей) — это атомарный, декларативный движок для Node.js, спроектированный для создания как **веб-сервисов**, так и **кросс-платформенных десктопных приложений** с помощью LLM (Large Language Models). Его главная задача — превратить LLM из "программиста" в "архитектора", который описывает **ЧТО** нужно сделать, а не **КАК**.

Движок позволяет создавать сложные, интерактивные и "живые" real-time интерфейсы, используя подход **"HTML-over-the-wire"**, без написания единой строчки JavaScript на фронтенде.

*   **Автор:** Xzdes
*   **Контакт:** [xzdes@yandex.ru](mailto:xzdes@yandex.ru)
*   **Репозиторий:** [https://github.com/Xzdes/serverokey](https://github.com/Xzdes/serverokey)

---

## 🎯 Философия: Архитектура вместо Кода

Современные LLM отлично генерируют код, но часто ошибаются в деталях, требующих контекста. Serverokey решает эту проблему, заменяя написание императивного кода на заполнение декларативных структур в центральном файле — `manifest.js`.

Это позволяет LLM сосредоточиться на своих сильных сторонах — проектировании архитектуры и потоков данных, — минимизируя вероятность ошибок и повышая предсказуемость результата.

1.  **Декларативные Коннекторы:** Вместо написания кода для подключения к БД, вы описываете подключение: `{"type": "wise-json", "collection": "users"}`. Это позволяет менять источник данных, не переписывая логику приложения.

2.  **Декларативная Логика (`steps`):** Вместо `if/else` в отдельном JS-файле, вы описываете сложную бизнес-логику прямо в манифесте: `{"if": "data.receipt.total > 300", "then": [...]}`. Это делает логику прозрачной и легко читаемой.

3.  **Полная Изоляция и "Эмпатия":** Движок абстрагирует всю сложную "обвязку", прощает мелкие синтаксические ошибки и дает полезные подсказки через валидатор. LLM не имеет прямого доступа к файловой системе или опасным API.

> **Результат:** LLM работает с **единой точкой входа** (`manifest.js`), генерируя предсказуемые, безопасные и легко проверяемые веб-приложения.

---

## 🛠️ Рабочий процесс: От Идеи до Результата

Serverokey предлагает уникальный рабочий процесс, идеально подходящий для быстрой итеративной разработки, в том числе с использованием AI-ассистентов.

### 1. Установка и запуск примера

Проект представляет собой монорепозиторий с двумя основными пакетами:
*   `packages/serverokey`: 📦 Ядро движка.
*   `packages/kassa-app-example`:  Пример приложения, использующего `serverokey`.

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/Xzdes/serverokey.git
cd serverokey

# 2. Установите все зависимости (включая Chromium для нативного режима)
npm install

# 3. Перейдите в папку с примером
cd packages/kassa-app-example

# 4. Наполните базу данных wise-json-db начальными данными
npm run seed

# 5. Запустите приложение!
npm run dev
```
В зависимости от настроек в `manifest.js`, приложение запустится либо как веб-сервер, либо как десктопное приложение.

### 2. Выбор режима запуска в `manifest.js`

Откройте `packages/kassa-app-example/manifest.js` и найдите секцию `launch`. Она управляет режимом запуска.

**Режим веб-сервера:**
```javascript
launch: {
  "mode": "server"
}
```
При запуске `npm run dev` приложение будет доступно в браузере по адресу `http://localhost:3000`.

**Нативный десктопный режим:**
```javascript
launch: {
  "mode": "native",
  "window": {
    "title": "Мое Десктопное Приложение",
    "width": 1280,
    "height": 800
  }
}
```
При запуске `npm run dev` откроется окно Chromium с вашим приложением.

### 3. Цикл разработки "Манифест -> Валидатор -> Результат"

Это сердце рабочего процесса Serverokey.

#### **Шаг 1: Редактирование `manifest.js`**
Любое изменение в приложении — добавление страницы, нового действия или источника данных — начинается с редактирования файла `manifest.js`.

#### **Шаг 2: Валидация (Критически важный шаг!)**
**Перед тем как проверять результат**, запустите встроенный "Супер-Валидатор". Он — ваш главный помощник и система безопасности.

```bash
# Находясь в папке приложения (например, kassa-app-example)
npm run validate
```
*   **Если вы видите:** `✅ Manifest validation successful. No issues found.` — значит, архитектура корректна.
*   **Если вы видите:** `🚨 Found X issues:` — валидатор предоставит вам **четкий и понятный список проблем** и даже предложит исправление (`Did you mean...?`).

#### **Шаг 3: Запуск и отладка**
После успешной валидации запустите сервер в режиме отладки. Он включает горячую перезагрузку и выводит массу полезной информации.

```bash
npm run dev:debug
```

---

## 📖 Справочник по `manifest.js`

`manifest.js` — это единый источник правды для всего приложения.

### `launch` (Режим запуска)
Определяет, как будет запущено приложение.
```javascript
launch: {
  "mode": "native", // 'server' или 'native'
  "window": { // Настройки для нативного режима
    "title": "Заголовок Окна",
    "width": 1280,
    "height": 800,
    "devtools": true // Открыть ли инструменты разработчика
  }
}
```

### `sockets` (Real-time)
Описывает real-time каналы для "живых" обновлений.
```javascript
sockets: {
  "receipt-updates": {
    "watch": "receipt", // Следить за изменениями в этом коннекторе
    "emit": { "event": "receipt-changed", "payload": "receipt" }
  }
}
```

### `connectors`
Описывает все источники данных. Все коннекторы, работающие с коллекциями, возвращают данные в едином формате `{ items: [...] }`.

### `routes`
Связывает URL и HTTP-метод с действием. Существует два типа роутов: `view` и `action`.

#### `type: 'view'`
Рендерит целую HTML-страницу.

#### `type: 'action'`
Выполняет серверную логику, описанную в **`steps`**.

**Контекст `steps` (Единый и Предсказуемый):**
Внутри `steps` вам доступны три главных объекта:
*   `data`: Объект, содержащий все данные из коннекторов, перечисленных в `reads`.
*   `user`: Объект текущего авторизованного пользователя.
*   `body`: Объект с данными, присланными с клиента.

**Основные шаги (`steps`):**
*   `{ "set": "...", "to": "..." }`: Устанавливает значение в контексте.
*   `{ "if": "...", "then": [...], "else": [...] }`: Выполняет условную логику.
*   `{ "run": "actionFileName" }`: Выполняет JS-файл из `app/actions/`.
*   `{ "action:run": { "name": "routeName" } }`: Выполняет другой `action`-роут.
*   `{ "auth:login": "..." }`: Авторизует пользователя.
*   `{ "auth:logout": true }`: Завершает сессию.
*   `{ "client:redirect": "'/path'" }` или `{ "client:redirect": "{ url: '/path', full: true }" }`: Перенаправляет браузер (SPA или полная перезагрузка).
*   `{ "http:get": { "url": "...", "saveTo": "..." } }`: Выполняет внешний GET-запрос.

---

## 템 Шаблоны и Клиентская часть

Движок использует **Mustache.js** для рендеринга и добавляет свои "атомарные" атрибуты.

*   **Атрибуты взаимодействия:**
    *   `atom-action="METHOD /url"`: Указывает, какой экшен вызвать.
    *   `atom-target="#css-selector"`: Указывает, какой контейнер на странице обновить.
*   **Real-time атрибуты:**
    *   `atom-socket="channel-name"`: Подписывает компонент на WebSocket-канал.
    *   `atom-on-event="event-name"`: Указывает, при каком событии с сервера нужно сработать.
*   **Условный рендеринг: `atom-if`**
    ```html
    <div class="error" atom-if="url.query.error">Неверный логин или пароль</div>
    ```

---

## 🔮 Будущее развитие

*   [ ] **Декларативный `find` в `steps`:** Добавление шага `{ "find": { "from": "...", "where": "...", "saveTo": "..." } }`.
*   [ ] **Интерактивный CLI-помощник:** Утилита для быстрого создания компонентов и экшенов.
*   [ ] **"Нативный мост":** Контролируемый доступ к API Node.js из нативного режима (диалоги файлов и т.д.).
*   [ ] **Сборка дистрибутивов:** Исследование упаковки нативных приложений в `.exe` / `.dmg`.

---

Этот проект — эксперимент в области взаимодействия человека и машины, направленный на создание надежных и предсказуемых программных систем. Буду рад вашим идеям и вкладу!

**Xzdes**